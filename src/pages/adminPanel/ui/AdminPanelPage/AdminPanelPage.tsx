import { memo } from 'react';

import { Page } from '@/shared/ui/depricated/Page';

const AdminPanelPage = memo(() => <Page data-testid="admin-panel-page">admin panel</Page>);

export default AdminPanelPage;
